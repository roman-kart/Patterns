# –еализаци€ Singleton при помощи пол€
## ќсобенности реализации Sinleton при помощи полей:
### ќбработка исключений
“.к. инициализаци€ singleton происходит в статическом конструкторе,
то в случае генерации исключени€ все клиенты получают его "завернутым" в TypeInitialisationException.
», в отличие от предыдущих реализаций, попытка инициализации singleton будет лишь одна.
### ¬рем€ создани€ Singleton
≈сли "забыть" пустой статический конструктор, то врем€ инициализации singleton станет недетерминированным. 
≈сли у типа не определен статический конструктор €вно, то компил€тор помечает тип атрибутом beforeFieldInit, 
что позволит вызвать сгенерированный статический конструктор отложенным (relaxed) образом задолго до первого обращени€ к singleton.
### "Ћенивость"
Ёта реализаци€ не полностью "ленива". 
»нициализаци€ такого sinlgleton происходит во врем€ вызова статического конструктора,
который может быть вызван не только при использовании singleton,
но и при обращении к статическому члену этого класса.

## ƒостоинства и недостатки
### ƒостоинства:
	- относительна€ простота реализации.
### Ќедостатки:
	- особенности генерации исключений;
	- возможные проблемы с "ленивостью" (без вложенности класса);
	- проблемы с временем инициализации при отсутствии статического конструктора.

